import React from "react";

const Item = ({Item}) => {

    
/*
let prom = new Promise (function(resolve, reject ) {
setTimeout (function() {
    resolve (array);
},2000);
} )
*/
}
export default Item;